# Terraform Modules

Reusable infrastructure modules for the Vantage Portal MSSP platform.

## Planned Modules

- `supabase/` — Supabase project + connection pooler config
- `container_apps/` — Azure Container Apps environment + apps
- `key_vault/` — Azure Key Vault + managed identity access policies
- `monitoring/` — Azure Monitor workspace + Log Analytics
